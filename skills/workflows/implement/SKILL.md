---
name: implement
description: Implement one approved ticket with TDD and evidence.
disable-model-invocation: true
---

# Implement

只实现一个 ready Ticket。读取 Prompt Card、批准 Seam 和 Spec；调用 `tdd` 逐切片实现；持续运行类型检查和相关测试；保存证据；调用 `code-review` 和 `repository-validation` 后才可完成。
