# Spec Deltas

产品和领域 Change 的 Spec 文件名使用对应领域 ID。跨领域工程、治理或安全 Change 使用与 Change 类型一致的稳定范围名，例如 `repository-governance.md`。

每项要求使用 `## SPEC-...` 标题，并以 `ADDED`、`MODIFIED`、`REMOVED` 或 `RENAMED` 表达增量。每项要求必须有全仓唯一稳定 ID 和可观察的验收场景。
