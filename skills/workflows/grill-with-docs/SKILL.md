---
name: grill-with-docs
description: Clarify a requirement and sharpen domain language before creating an OpenSpec change.
disable-model-invocation: true
---

# Grill With Docs

通过具体场景和边界案例消除歧义。调用 `domain-modeling` 更新 glossary、领域资产或 Decision；将尚未决定的问题写入 Change `context.md`，不直接进入实现。
