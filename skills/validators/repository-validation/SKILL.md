---
name: repository-validation
description: Run deterministic repository structure and schema validation.
---

# Repository Validation

执行 `python tools/validate_repo.py`。不得通过修改 Schema、跳过目录或降低检查来使门禁通过；规则变更必须单独评审。
