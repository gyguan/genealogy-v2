---
name: to-tickets
description: Break an approved OpenSpec change into tracer-bullet tickets.
disable-model-invocation: true
---

# To Tickets

将批准的 Spec 拆成可在一个新上下文窗口完成、可独立演示和验证的纵向切片。维护 `tasks.md` 与 `implementation/tracer-tickets.yaml`，显式记录阻塞边。
