# Genealogy V2 工程规则

本文件是仓库的最高级协作规则。任何产品设计、架构设计、Issue 拆分和后续实现都必须遵守。

## 1. 当前阶段

当前仓库处于 **文档与领域基线阶段**。

在以下内容完成评审前，不进入大规模业务编码：

- 产品范围和非目标；
- 领域事实与业务不变量；
- 正式事实生效路径；
- 模块边界和依赖方向；
- 权限、隐私和审计模型；
- 首批 API 用例和验收标准。

## 2. P0 设计红线

1. 不复制 `gyguan/genealogy` 的业务代码、表结构、DTO 或兼容层。
2. 人物身份、人物姓名、谱系归属必须分离。
3. 血缘关系、家庭关系、谱系组织关系必须分离。
4. 当前组织状态与历史组织事实必须分离。
5. 历史发生时间与系统记录时间必须分离。
6. 来源、引文、主张、证据和正式事实必须分离。
7. 正式事实只能通过明确的业务命令和审核生效。
8. 禁止使用无法建立数据库约束的通用 `target_type + target_id` 作为核心事实关联。
9. 权限必须由后端按角色、业务范围、对象状态和隐私策略共同裁决。
10. 在世人物及其敏感字段默认最小披露。
11. 图谱模块只读取正式事实，不承担正式事实写入。
12. 不为旧实现保留永久兼容成本。

## 3. 文档权威性

每类规则只能有一个权威来源：

| 内容 | 权威文档 |
|---|---|
| 产品范围和核心场景 | `docs/product/product-definition.md` |
| 领域对象和业务不变量 | `docs/product/domain-model.md` |
| 系统结构和技术边界 | `docs/architecture/system-architecture.md` |
| 前端页面和视觉规范 | `docs/frontend/design-system.md` |
| API 规则 | `docs/standards/api-design.md` |
| 数据库规则 | `docs/standards/database-design.md` |
| 权限与隐私 | `docs/standards/authorization-and-privacy.md` |
| 交付过程 | `docs/governance/document-driven-development.md` |
| 实施顺序 | `docs/roadmap/implementation-roadmap.md` |

README 只负责导航，不重复定义详细规则。

## 4. 设计与实现流程

非平凡变更遵循：

```text
DEFINE → MODEL → CONTRACT → PLAN → BUILD → VERIFY → REVIEW
```

- DEFINE：明确用户问题、范围、非目标和验收标准。
- MODEL：明确事实、对象、状态、命令和不变量。
- CONTRACT：明确 API、事件或数据契约，不先写 Controller 或表。
- PLAN：拆分可独立验证的 Issue，并给出依赖顺序。
- BUILD：在已批准边界内实现。
- VERIFY：执行单元、集成、契约、安全和端到端验证。
- REVIEW：检查领域正确性、边界、兼容性和风险。

## 5. Ask First

以下事项必须先形成设计说明并完成评审：

- 新增或修改核心领域对象；
- 数据库 Schema 和历史数据策略；
- 公共 API、导入导出格式；
- 认证、授权、隐私和审计；
- 正式事实审核与生效路径；
- 图谱数据结构和查询上限；
- 引入新的框架、存储或运行组件；
- 跨模块写事务和删除策略。

## 6. Never Do

- 不为展示进度提交占位业务代码。
- 不提交未经 PostgreSQL 集成验证的核心建表脚本。
- 不为了“完整”提前定义大量没有用例支撑的 API。
- 不让前端自行推断亲子、配偶、字辈、隐私或审核结果。
- 不以 JSON 大字段代替稳定领域模型。
- 不以软删除代替历史事实建模。
- 不绕过审核直接修改正式事实。
- 不静默扩大 Issue 范围。
- 不降低或删除测试来通过门禁。

## 7. 文档质量要求

设计文档必须回答：

1. 解决什么用户或业务问题？
2. 哪些事实由哪个模块负责？
3. 关键不变量是什么？
4. 哪些内容明确不在范围内？
5. 失败、冲突和补偿如何处理？
6. 权限和隐私如何裁决？
7. 如何验证已经正确实现？

无法回答这些问题的文档不得进入权威基线。
